<%= form_for(@comment, url: micropost_comments_path(@micropost.id)) do |f| %>
  <div class="field">
    <%= f.text_area :content, placeholder: "コメントを送信してみよう！" %>
    <%= f.hidden_field :user_id, :value => current_user.id %>
  </div>
  <%= f.submit "送信する", class: "btn btn-primary" %>
<% end %>