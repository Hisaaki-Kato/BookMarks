<% provide(:title, 'All users') %>
<div class="container">
  <h1>ユーザー一覧</h1>

  <%= paginate @users %>

  <ul class="users">
    <%= render @users %>
  </ul>

  <%= paginate @users %>
</div>