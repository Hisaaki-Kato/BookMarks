<%= form_for(@board, url: yield(:url)) do |f| %>
  <%= render 'shared/error_messages', object: @board %>

  <%= f.label :content %>
  <%= f.text_field :content, class: 'form-control' %>

  <%= f.submit yield(:button_text), class: "btn btn-primary" %>
<% end %>