<% provide(:title, 'HOME') %>
<div class="row">
  <aside class="col-md-4">
    <section class="user_info">
      <h1>人気の書籍</h1>
      <%= render @popular_books %>
      <p><%= link_to "他の本を探す >>", new_book_path %></p>
    </section>
  </aside>

  <div class="col-md-8">
    <h1>最新の投稿</h1>

    <%= will_paginate @microposts %>
    <ul class="microposts">
      <%= render @microposts %>
    </ul>

    <%= will_paginate @microposts %>
  </div>
</div>